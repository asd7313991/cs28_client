VITE_APP_NAME=CS28 Dev
# 前端运行在 http://localhost:5173
# 开发环境使用 Vite 代理，把 /api 和 /ws 转发到本机 FastAPI
VITE_PROXY_TARGET=http://localhost:8000

# 仅为类型统一，开发阶段 axios 使用 /api（由代理转发），这两个不会被用到
VITE_API_BASE=/api
VITE_WS_BASE=/ws
VITE_SEAL_LEAD_MS=30000